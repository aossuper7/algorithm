#include <iostream>
using namespace std;
int main() {
	ios_base::sync_with_stdio(false); cin.tie(nullptr); cout.tie(nullptr);
	int c;
	cin >> c;
	for (int i = 1; i <= c; i++)
		cout << "Hello World, Judge " << i << "!\n";
}